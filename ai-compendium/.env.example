# Public URL used in password-reset emails (no trailing slash)
PUBLIC_BASE_URL=http://localhost:3000

# Mailpit defaults (run: docker run -d --name mailpit -p 8025:8025 -p 1025:1025 axllent/mailpit)
SMTP_HOST=127.0.0.1
SMTP_PORT=1025
# SMTP_SECURE=true
# SMTP_USER=
# SMTP_PASS=
SMTP_FROM=AI Compendium <noreply@localhost>
