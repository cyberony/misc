# AI Compendium — natural next steps (reference)

**Natural next steps** (pick one or mix):

1. **Features** — edit/delete resources, duplicate check, export JSON, richer examples (links), dark mode  
2. **Deploy** — Docker, Fly/Railway/Render, or static+server split  
3. **“Agentic”** — ingest URLs, auto-tag from description, or a small CLI to append resources  
4. **Quality** — validation, rate limits, basic auth for writes, tests  
5. **UX** — keyboard nav, shareable filter URLs (`?tag=…`), empty states, mobile polish  

What do you want to focus on first (e.g. “add edit/delete” or “Docker + deploy”)? If you’re not sure, say your priority: **ship publicly**, **team use**, or **personal catalog** and we’ll align the work to that.
